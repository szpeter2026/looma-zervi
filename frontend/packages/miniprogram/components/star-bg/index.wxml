<view class="star-bg">
  <view
    wx:for="{{stars}}"
    wx:key="index"
    class="star"
    style="left: {{item.left}}%; top: {{item.top}}%; width: {{item.size}}rpx; height: {{item.size}}rpx; animation-delay: {{item.delay}}s; animation-duration: {{item.duration}}s;"
  ></view>
</view>
